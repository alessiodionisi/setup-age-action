{
  "name": "setup-age-action",
  "version": "1.4.0",
  "description": "Setup age and add it to the PATH",
  "main": "./dist/index.js",
  "repository": "git@github.com:alessiodionisi/setup-age-action.git",
  "author": "Alessio Dionisi <hello@adns.io>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "esbuild src/main.ts --bundle --platform=node --target=node24 --outfile=dist/index.js --legal-comments=linked",
    "format": "prettier --write \"**/*.ts\""
  },
  "devDependencies": {
    "@types/node": "^24.12.0",
    "@types/semver": "^7.7.1",
    "esbuild": "^0.25.0",
    "prettier": "^3.8.1",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@actions/core": "^3.0.0",
    "@actions/github": "^9.0.0",
    "@actions/tool-cache": "^4.0.0",
    "semver": "^7.7.4"
  }
}
